name: Deploy Images to GHCR

on:
  push:
    branches:
      - main

jobs:
  push-store-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: 'Checkout dockerfile'
        uses: actions/checkout@main
